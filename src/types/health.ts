export interface HealthCategory {
  id: string;
  title: string;
  emoji: string;
  description: string;
}

export interface GeneratedContent {
  title: string;
  content: string;
  imageUrl: string;
}
